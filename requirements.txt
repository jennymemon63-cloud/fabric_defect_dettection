streamlit

# Force headless OpenCV first
opencv-python-headless==4.8.1.78

# Specific versions for compatibility
ultralytics==8.0.196
numpy==1.26.4
pillow

# Torch CPU versions (don’t use +cpu in version, let pip resolve from index-url)
torch==2.2.0
torchvision==0.17.0

--extra-index-url https://download.pytorch.org/whl/cpu
